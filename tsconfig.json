{
  "compilerOptions": {
    /* Basic Options */
    "target": "es2017",
    "module": "commonjs",
    "lib": ["es2015", "dom"],
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    // https://github.com/microsoft/TypeScript/wiki/Performance#controlling-types-inclusion:
    "types": ["jest"],
    // Prevent developers on different OSes from running into problems:
    "forceConsistentCasingInFileNames": true,
    /* Advanced Options */
    "stripInternal": true,
    /* Experimental Options */
    "skipLibCheck": true // temporarily skip lib checks due to a solid-client bug
  },
  "typedocOptions": {
    "out": "docs/api/source/api",
    "entryPoints": [
      // The source files of everything listed under `exports` in our package.json
      // (i.e. public API's that should be documented) should be listed here:
      "src/index.ts",
      "src/discover/index.ts",
      "src/fetch/index.ts",
      "src/manage/index.ts",
      "src/request/index.ts",
      "src/resource/index.ts",
      "src/verify/index.ts",
      // These types are reused across files, so give them a specific page for
      // them to be documented on:
      "src/type/AccessModes.ts",
      "src/type/FetchOptions.ts",
      "src/type/UmaConfiguration.ts",
      "src/type/AccessBaseOptions.ts",
      "src/type/IssueAccessRequestParameters.ts",
      "src/type/RedirectOptions.ts",
      "src/type/Parameter.ts"
    ],
    "exclude": [
      "node_modules/**",
      ".codesandbox/**",
      "**/*.test.ts",
      // Internal helpers:
      "**/*.internal.ts",
      // Re-exported functions are already documented in their own modules:
      "src/index.ts",
      // Constants are only used internally:
      "src/constants.ts"
    ],
    "theme": "markdown",
    "readme": "none",
    "hideInPageTOC": true,
    "entryDocument": "index.md",
    // This is used to get source URLs working on Vercel, where the git remote
    // isn't automatically detected. See the nlfurniss-typedoc-plugin-sourcefile-url plugin
    "sourcefile-url-prefix": "https://github.com/inrupt/solid-client-access-grants-js/blob/main/"
  },
  "include": ["src/index.ts", ".eslintrc.js"],
  "exclude": [
    "**/node_modules",
    // These end-to-end tests reference code in `.codesandbox`,
    // which is not part of this project and should not be compiled together with it.
    // Not excluding this will lead to the root of the repository being used
    // as the root directory for compilation (instead of /src),
    // meaning that files that would otherwise be included in /dist directly
    // (e.g. dist/index.d.ts) will then be built to /dist/src.
    "e2e",
    "examples"
  ]
}
